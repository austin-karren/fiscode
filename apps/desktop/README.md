# desktop

Placeholder for the Electrobun desktop wrapper (part 3).

Planned: wrap the built `apps/web` SPA in an Electrobun native window. Mirror
`/Users/austinkarren/workspaces/personal/fiscode-reference/apps/desktop`:
`dev:hmr`, `build:stable`, `build:canary`.

The local-only architecture means there is no backend coupling to undo —
Electrobun just hosts the same OPFS-backed SPA in a native window.
