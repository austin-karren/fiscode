---
title: Income
description: 1099 and W-2 entries, per payment.
---

Edit at `/income`. `source_type` is `1099` or `w2`. Amounts are stored in integer cents. Entries
are dated so the quarterly engine can bucket them correctly.
